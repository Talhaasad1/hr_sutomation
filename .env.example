MONGO_URI=mongodb://localhost:27017
DB_NAME=ats_portal
ANTHROPIC_API_KEY=

# Only used by docker-compose.yml (sets up MongoDB's root user inside the container)
MONGO_ROOT_USER=atsadmin
MONGO_ROOT_PASSWORD=change_this_to_a_strong_password
